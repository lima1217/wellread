export declare function normalizeSkillRel(relPath: string): string;

export declare function isBundledOnlySkillRel(relPath: string): boolean;

/** Alias kept for sidecar call sites that historically used this name. */
export declare function preferBundledSkillRel(relPath: string): boolean;

export declare function canonicalBundledSkillRel(relPath: string): string;
