{
  "name": "@wellread/skill-contract",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bundled-only skill package path SSOT (host import + eve sidecar overlay).",
  "exports": {
    ".": {
      "types": "./src/index.d.ts",
      "default": "./src/index.mjs"
    }
  },
  "scripts": {
    "test": "node --test src/**/*.test.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
